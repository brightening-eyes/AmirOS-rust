name: QEMU smoke

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  x86_64:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src
      - uses: Swatinem/rust-cache@v2
      - name: Install ISO and VM tools
        run: sudo apt-get update && sudo apt-get install -y xorriso qemu-system-x86
      - name: Build kernel
        run: cargo build --release
      - name: Smoke boot in QEMU
        env:
          SMOKE_MARKER: allocator initialized
          SMOKE_SECOND_MARKER: timer: ticks=
          SMOKE_THIRD_MARKER: ioapic selftest passed
        run: bash scripts/qemu-smoke.sh x86_64-unknown-none release 120
